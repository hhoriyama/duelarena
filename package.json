{
  "name": "duel-arena",
  "version": "0.1.0",
  "private": true,
  "description": "デュエルアリーナ - Discord連携の対戦マッチング・レーティング管理システム",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix web",
    "dev:backend": "npm run dev --prefix backend",
    "dev:web": "npm run dev --prefix web",
    "build:backend": "npm run build --prefix backend",
    "build:web": "npm run build --prefix web",
    "build": "npm run build:backend && npm run build:web",
    "test": "npm run test:backend",
    "test:backend": "npm test --prefix backend",
    "test:integration": "npm run test:integration --prefix backend",
    "typecheck": "npm run typecheck --prefix backend && npm run typecheck --prefix web"
  }
}
